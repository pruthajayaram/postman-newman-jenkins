{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "To store post man collection",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-tests-integration": "newman run tests/Try_Jenkins.postman_collection_new.json --reporters cli,htmlextra --reporter-htmlextra-export ./results/report.html"
  },
  "author": "Prutha",
  "dependencies": {
    "newman": "^5.2.0"
  }
}
